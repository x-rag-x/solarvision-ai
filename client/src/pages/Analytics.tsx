import { trpc } from "@/lib/trpc";
import { BarChart3, ChartNoAxesCombined, Layers3 } from "lucide-react";
import { Bar, BarChart, CartesianGrid, Cell, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { DataSourceNote, EmptyData, PageIntro } from "@/components/solarvision/Primitives";

const colors = ["#0bd3b0", "#e3a336", "#7a7ff5", "#db6c91", "#5b9be8", "#8abf66"];

export default function Analytics() {
  const { data, isLoading } = trpc.analytics.defects.useQuery();
  const byType = data?.byType ?? [];
  const recentTrend = data?.recentTrend ?? [];
  const configured = Boolean(data?.configured);
  const hasTypes = byType.length > 0;
  const hasTrend = recentTrend.length > 0;
  return <div><PageIntro eyebrow="Quality intelligence" title="Defect analytics." description="Understand the labels and throughput that the inspection database has actually observed. No synthetic production or sensor data is shown." /><DataSourceNote configured={configured} />{isLoading ? <div className="rounded-2xl bg-white p-8 text-sm text-slate-400">Loading analytics…</div> : <div className="grid gap-5 xl:grid-cols-[1.15fr_0.85fr]"><div className="rounded-2xl border border-slate-200/80 bg-white p-6 shadow-[0_10px_30px_rgba(7,27,41,0.04)]"><div className="mb-6 flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-[#079a86]">By defect type</p><h2 className="mt-2 text-xl font-semibold tracking-[-0.03em]">Observed detections</h2></div><div className="grid h-10 w-10 place-items-center rounded-xl bg-[#e8fbf7] text-[#079a86]"><BarChart3 className="h-5 w-5" /></div></div>{hasTypes ? <div className="h-[340px] w-full"><ResponsiveContainer width="100%" height="100%"><BarChart data={byType} margin={{ top: 8, right: 8, bottom: 8, left: -18 }}><CartesianGrid vertical={false} stroke="#e7eef0" /><XAxis dataKey="defectType" axisLine={false} tickLine={false} tick={{ fill: "#94a3b8", fontSize: 10 }} interval={0} angle={-28} textAnchor="end" height={68} /><YAxis axisLine={false} tickLine={false} tick={{ fill: "#94a3b8", fontSize: 10 }} allowDecimals={false} /><Tooltip cursor={{ fill: "#f6f9fa" }} contentStyle={{ border: "1px solid #e7eef0", borderRadius: 12, fontSize: 12 }} /><Bar dataKey="count" radius={[6, 6, 0, 0]}>{byType.map((entry, index) => <Cell key={entry.defectType} fill={colors[index % colors.length]} />)}</Bar></BarChart></ResponsiveContainer></div> : <EmptyData title="No defect data available" description="Complete an inspection with the configured YOLO model to calculate defect counts." />}</div><div className="rounded-2xl border border-slate-200/80 bg-white p-6 shadow-[0_10px_30px_rgba(7,27,41,0.04)]"><div className="mb-6 flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-[#079a86]">Run trend</p><h2 className="mt-2 text-xl font-semibold tracking-[-0.03em]">Daily inspection volume</h2></div><div className="grid h-10 w-10 place-items-center rounded-xl bg-[#fff4df] text-[#a96808]"><ChartNoAxesCombined className="h-5 w-5" /></div></div>{hasTrend ? <div className="h-[340px] w-full"><ResponsiveContainer width="100%" height="100%"><LineChart data={[...recentTrend].reverse()} margin={{ top: 8, right: 8, bottom: 8, left: -18 }}><CartesianGrid vertical={false} stroke="#e7eef0" /><XAxis dataKey="day" axisLine={false} tickLine={false} tick={{ fill: "#94a3b8", fontSize: 10 }} /><YAxis axisLine={false} tickLine={false} tick={{ fill: "#94a3b8", fontSize: 10 }} allowDecimals={false} /><Tooltip contentStyle={{ border: "1px solid #e7eef0", borderRadius: 12, fontSize: 12 }} /><Line type="monotone" dataKey="inspections" stroke="#071b29" strokeWidth={2.5} dot={{ r: 3, fill: "#0bd3b0", strokeWidth: 0 }} activeDot={{ r: 5 }} /></LineChart></ResponsiveContainer></div> : <EmptyData title="No trend data available" description="The trend will be calculated from inspection timestamps after the first stored run." />}</div></div>}{hasTypes && <div className="mt-5 rounded-2xl border border-slate-200/80 bg-white p-6 shadow-[0_10px_30px_rgba(7,27,41,0.04)]"><div className="mb-5 flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-xl bg-[#f1efff] text-[#666bdc]"><Layers3 className="h-4 w-4" /></div><div><h2 className="text-base font-semibold">Confidence by label</h2><p className="text-xs text-slate-400">Average confidence is calculated from returned detections.</p></div></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{byType.map((item, index) => <div key={item.defectType} className="rounded-xl bg-[#f6f9fa] p-4"><div className="flex items-center justify-between gap-3"><p className="truncate text-xs font-semibold text-slate-600">{item.defectType}</p><span className="h-2 w-2 rounded-full" style={{ backgroundColor: colors[index % colors.length] }} /></div><p className="mt-3 font-mono text-lg font-semibold text-slate-800">{(Number(item.avgConfidence) * 100).toFixed(1)}%</p><p className="mt-1 text-[11px] text-slate-400">{item.count} detections</p></div>)}</div></div>}</div>;
}
